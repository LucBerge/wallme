language: python

python:
    - 3.5
    - 3.6
    - 3.7
    - 3.8
    
install:
    - pip install requests bs4 Pillow pathlib

script:
    - travis_wait pytest --cov=test/

after_success:
  - bash <(curl -s https://codecov.io/bash)